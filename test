## integer or Arithmetic comparisons

if [ "$a" -eq "$b" ]

if [ "$a" -ne "$b" ]

if [ "$a" -gt "$b" ]

if [ "$a" -ge "$b" ]

if [ "$a" -lt "$b" ]

if [ "$a" -le "$b" ]

if (("$a" < "$b"))

if (("$a" <= "$b"))

if (("$a" > "$b"))

if (("$a" >= "$b"))

## String comparisons

if [ "$a" = "$b" ]

